<div class="container">
  <div class="row">
    <div class="col-md-12">
      <%= paginate @root_articles %>  
    </div>
  </div>
  <div class="row">
    <div class="col-md-12">
      <% @root_articles.each do |root_article| %>
        <%= render "dashboard/article", article: root_article.articles.first unless root_article.articles.empty? %>
      <% end %>
    </div>
  </div>
  <div class="row">
    <div class="col-md-12">
      <%= paginate @root_articles %>
    </div>
  </div>
</div>
<% 
  meta title: normalize_title(t('static_pages.navbar.programs.title_html')),
        description: t('static_pages.metatags.programs.description'),
        og: {
          description: t('static_pages.metatags.programs.description'),
          image: "http://www.deroseforleaders.com/assets/logo_derose_method.png",
          url: group_consultory_url,
          title: (normalize_title(t('static_pages.navbar.programs.title_html')))
        },
        twitter: {
          card: "summary",
          description: t('static_pages.metatags.programs.description'),
          site: "@deroseforleaders",
          image: "http://www.deroseforleaders.com/assets/logo_derose_method.png"
        }
%>

<%= render 'group_consultory_header' %>
<%= render 'group_consultory_body' %>
<%= render 'partners' %>

<script type="text/javascript">
  $(function() {
    // Javascript to enable link to tab
    var url = document.location.toString();
    if (url.match('#')) {
      $('.nav-pills a[href=#'+url.split('#')[1]+']').tab('show') ;
    }

    // Change hash for page-reload
    $('a[data-toggle="tab"]').on('show.bs.tab', function (e) {
      window.location.hash = e.target.hash;
    });
  });
</script>